# คู่มือการใช้งาน Automated Testing ภาษาไทย

## 🎯 เริ่มต้นใช้งานอย่างรวดเร็ว

### ติดตั้งครั้งแรก

1. **ติดตั้ง Dependencies**
```bash
npm install
```

2. **ติดตั้ง Browsers สำหรับ Playwright**
```bash
npx playwright install
```

3. **รัน Test ครั้งแรก**
```bash
npm test
```

## 📖 วิธีการรัน Tests

### วิธีที่ 1: ใช้ Batch File (Windows)
สำหรับผู้ใช้ Windows สามารถดับเบิลคลิกที่ `run-tests.bat` แล้วเลือกตัวเลือกที่ต้องการ

### วิธีที่ 2: ใช้คำสั่ง npm
```bash
# รัน test ทั้งหมด
npm test

# รัน test แบบเห็น browser
npm run test:headed

# รัน test แบบ UI Mode (แนะนำ!)
npm run test:ui

# ดู test report
npm run test:report

# รัน test แบบ debug
npm run test:debug
```

### วิธีที่ 3: ใช้คำสั่ง Playwright โดยตรง
```bash
# รัน test ทั้งหมด
npx playwright test --workers=1

# รัน test ไฟล์เดียว
npx playwright test login.spec.js --workers=1

# รัน test เฉพาะ browser
npx playwright test --project=chromium --workers=1
npx playwright test --project=firefox --workers=1
npx playwright test --project=webkit --workers=1

# รัน test แบบเห็น browser
npx playwright test --headed --workers=1

# รัน test ที่มีคำว่า "login" ในชื่อ
npx playwright test -g "login" --workers=1
```

## 📂 โครงสร้างโปรเจค

```
test-01/
├── tests/                          # โฟลเดอร์เก็บไฟล์ test ทั้งหมด
│   ├── login.spec.js              # ทดสอบการ Login (7 tests)
│   ├── products.spec.js           # ทดสอบหน้า Products (9 tests)
│   ├── cart.spec.js               # ทดสอบตะกร้าสินค้า (11 tests)
│   ├── checkout.spec.js           # ทดสอบการชำระเงิน (13 tests)
│   ├── e2e-complete-flow.spec.js  # ทดสอบ End-to-End (5 tests)
│   └── example-quick-test.spec.js # ตัวอย่างการเขียน test
│
├── playwright.config.js            # ไฟล์ configuration
├── package.json                    # ไฟล์จัดการ dependencies
├── run-tests.bat                   # ไฟล์สำหรับรัน tests บน Windows
├── README.md                       # คู่มือภาษาอังกฤษ
└── GUIDE-TH.md                    # คู่มือภาษาไทย (ไฟล์นี้)
```

## 🧪 Test Cases ทั้งหมด

### 1. Login Tests (login.spec.js) - 7 tests
- ✅ แสดงหน้า Login ได้ถูกต้อง
- ✅ Login สำเร็จด้วย standard_user
- ✅ แสดง Error สำหรับ locked_out_user
- ✅ แสดง Error สำหรับ username/password ผิด
- ✅ แสดง Error เมื่อ username ว่าง
- ✅ แสดง Error เมื่อ password ว่าง
- ✅ ปิด Error message ได้

### 2. Products Tests (products.spec.js) - 9 tests
- ✅ แสดงสินค้าทั้งหมด (6 items)
- ✅ เรียงสินค้าตามชื่อ A-Z
- ✅ เรียงสินค้าตามชื่อ Z-A
- ✅ เรียงสินค้าตามราคาต่ำไปสูง
- ✅ เรียงสินค้าตามราคาสูงไปต่ำ
- ✅ ดูรายละเอียดสินค้าได้
- ✅ กลับจากหน้ารายละเอียดสินค้า
- ✅ แสดง badge ตะกร้าสินค้า
- ✅ เปิด menu ได้

### 3. Cart Tests (cart.spec.js) - 11 tests
- ✅ เพิ่มสินค้าลงตะกร้าจากหน้า Products
- ✅ เพิ่มหลายสินค้าลงตะกร้า
- ✅ ลบสินค้าออกจากตะกร้า
- ✅ เพิ่มสินค้าจากหน้ารายละเอียดสินค้า
- ✅ เปิดหน้าตะกร้าสินค้า
- ✅ แสดงสินค้าในตะกร้าถูกต้อง
- ✅ แสดงหลายสินค้าในตะกร้า
- ✅ ลบสินค้าในหน้าตะกร้า
- ✅ Continue Shopping ได้
- ✅ ไปหน้า Checkout ได้
- ✅ สินค้ายังอยู่หลัง Logout/Login

### 4. Checkout Tests (checkout.spec.js) - 13 tests
- ✅ แสดงฟอร์มข้อมูลการจัดส่ง
- ✅ กรอกข้อมูลสำเร็จ
- ✅ Error เมื่อไม่กรอก First Name
- ✅ Error เมื่อไม่กรอก Last Name
- ✅ Error เมื่อไม่กรอก Postal Code
- ✅ ยกเลิกจากหน้าข้อมูล
- ✅ แสดงหน้า Overview ถูกต้อง
- ✅ คำนวณราคาถูกต้อง
- ✅ ยกเลิกจากหน้า Overview
- ✅ สั่งซื้อสำเร็จ
- ✅ ตะกร้าว่างหลังสั่งซื้อ
- ✅ กลับหน้า Products หลังสั่งซื้อ
- ✅ สั่งซื้อหลายสินค้าได้

### 5. E2E Tests (e2e-complete-flow.spec.js) - 5 tests
- ✅ การช้อปปิ้งทั้งหมด Login → Checkout
- ✅ ทดสอบกับ problem_user
- ✅ ทดสอบกับ performance_glitch_user
- ✅ ตรวจสอบ Visual Elements
- ✅ การรักษาสถานะขณะนำทาง

## 👤 User Accounts สำหรับทดสอบ

| Username | Password | คำอธิบาย |
|----------|----------|----------|
| `standard_user` | `secret_sauce` | User ปกติ ใช้ได้ทุกฟีเจอร์ |
| `locked_out_user` | `secret_sauce` | User ที่ถูกล็อค (ใช้ทดสอบ Error) |
| `problem_user` | `secret_sauce` | User ที่มีปัญหา UI |
| `performance_glitch_user` | `secret_sauce` | User ที่ช้า (ทดสอบ Performance) |
| `error_user` | `secret_sauce` | User ที่เจอ Error บางส่วน |
| `visual_user` | `secret_sauce` | User ที่มีปัญหา Visual |

## 🎨 Playwright UI Mode (แนะนำ!)

UI Mode เป็นฟีเจอร์ที่ดีที่สุดสำหรับการดู Debug และทำความเข้าใจ tests:

```bash
npm run test:ui
```

**คุณสมบัติ:**
- 👁️ ดู test ทำงานแบบ step-by-step
- 🔍 ดู DOM และ Network requests
- ⏯️ เล่น/หยุด/ข้าม test ได้
- 🐛 Debug แบบละเอียด
- 📸 ดู screenshots และ videos

## 📊 Test Reports

หลังจากรัน test เสร็จ Playwright จะสร้าง HTML Report อัตโนมัติ:

```bash
npm run test:report
```

**Report จะแสดง:**
- ✅ จำนวน Test ที่ผ่าน/ไม่ผ่าน
- ⏱️ เวลาที่ใช้ในแต่ละ Test
- 📸 Screenshots (เมื่อ Test ล้มเหลว)
- 🎥 Videos (เมื่อ Test ล้มเหลว)
- 📜 Traces สำหรับ Debug

## 🔧 การ Debug Tests

### วิธีที่ 1: ใช้ UI Mode (แนะนำ)
```bash
npm run test:ui
```

### วิธีที่ 2: ใช้ Debug Mode
```bash
npm run test:debug
```

### วิธีที่ 3: ใส่ Breakpoint ในโค้ด
เพิ่ม `await page.pause()` ในไฟล์ test:
```javascript
test('my test', async ({ page }) => {
  await page.goto('https://www.saucedemo.com');
  await page.pause(); // Test จะหยุดตรงนี้
  // ... โค้ดต่อไป
});
```

### วิธีที่ 4: ใช้ Console.log
```javascript
test('my test', async ({ page }) => {
  const title = await page.title();
  console.log('Page title:', title);
});
```

## 📝 การเขียน Test ใหม่

### ขั้นตอนพื้นฐาน:

1. **สร้างไฟล์ test ใหม่** ในโฟลเดอร์ `tests/`
   ชื่อไฟล์ต้องลงท้ายด้วย `.spec.js` เช่น `my-test.spec.js`

2. **Import Playwright**
```javascript
const { test, expect } = require('@playwright/test');
```

3. **เขียน Test**
```javascript
test('ชื่อ test', async ({ page }) => {
  // โค้ดทดสอบของคุณ
  await page.goto('https://www.saucedemo.com');
  await expect(page).toHaveTitle(/Swag Labs/);
});
```

4. **รัน Test**
```bash
npx playwright test my-test.spec.js --workers=1
```

### ตัวอย่างการเขียน Test:

ดูได้ที่ไฟล์ `tests/example-quick-test.spec.js` ซึ่งมีตัวอย่าง 7 แบบ:
1. ตรวจสอบ Title
2. Login พื้นฐาน
3. ถ่ายภาพหน้าจอ
4. รอ Element
5. นับจำนวน Elements
6. ดึงข้อความจาก Element
7. ใช้ Custom Timeout

## 🚀 Tips & Tricks

### 1. รัน Test ทีละไฟล์
```bash
npx playwright test login.spec.js --workers=1
```

### 2. รัน Test เฉพาะชื่อที่มีคำว่า "login"
```bash
npx playwright test -g "login" --workers=1
```

### 3. รันแบบเห็น Browser (Headed Mode)
```bash
npx playwright test --headed --workers=1
```

### 4. รันแบบช้าลง (Slow Motion)
ใส่ใน `playwright.config.js`:
```javascript
use: {
  launchOptions: {
    slowMo: 1000 // ช้าลง 1 วินาทีต่อ action
  }
}
```

### 5. รันเฉพาะ Browser เดียว
```bash
npx playwright test --project=chromium --workers=1
```

### 6. บันทึก Screenshot ทุกครั้ง
ใส่ใน `playwright.config.js`:
```javascript
use: {
  screenshot: 'on' // หรือ 'only-on-failure'
}
```

### 7. บันทึก Video ทุกครั้ง
ใส่ใน `playwright.config.js`:
```javascript
use: {
  video: 'on' // หรือ 'retain-on-failure'
}
```

## ❓ การแก้ปัญหาที่พบบ่อย

### ปัญหา: Test timeout
**แก้ไข:** เพิ่ม timeout ในไฟล์ test
```javascript
test.setTimeout(60000); // 60 วินาที
```

### ปัญหา: Element ไม่เจอ
**แก้ไข:** ใช้ `waitForSelector`
```javascript
await page.waitForSelector('#element-id');
```

### ปัญหา: Test ล้มเหลวแบบสุ่ม (Flaky)
**แก้ไข:** 
- เพิ่ม explicit waits
- ใช้ `waitForLoadState`
```javascript
await page.waitForLoadState('networkidle');
```

### ปัญหา: ไม่สามารถติดตั้ง browsers ได้
**แก้ไข:** รันคำสั่งนี้ด้วยสิทธิ์ Administrator
```bash
npx playwright install --with-deps
```

## 📚 เอกสารเพิ่มเติม

- [Playwright Official Docs](https://playwright.dev)
- [Playwright API Reference](https://playwright.dev/docs/api/class-playwright)
- [Best Practices](https://playwright.dev/docs/best-practices)
- [Debugging Guide](https://playwright.dev/docs/debug)

## 💬 ติดต่อและสนับสนุน

หากมีคำถามหรือปัญหา:
1. อ่าน [Playwright Documentation](https://playwright.dev)
2. ดู [GitHub Issues](https://github.com/microsoft/playwright/issues)
3. ถาม ChatGPT หรือ AI Assistant

---

**สร้างด้วย ❤️ โดยใช้ Playwright และ JavaScript**

*สุขสันต์ในการทดสอบ! 🎉*
